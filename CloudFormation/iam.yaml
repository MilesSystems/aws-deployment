AWSTemplateFormatVersion: 2010-09-09
Description: Deploys IAM based resources for EC2 + IMAGE BUILDER

Resources:
  EC2RoleForSSM:
    Type: AWS::IAM::Role
    Properties:
      RoleName: "EC2RoleForSSM"
      Policies:
        - PolicyName: "CompleteLifecycleActionAllowPolicy"
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: "Allow"
                Action: "autoscaling:CompleteLifecycleAction"
                Resource: "*"
        - PolicyName: "DescribeInstancesPolicy"
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: "Allow"
                Action:
                  - "ec2:DescribeInstances"
                  - "autoscaling:DescribeAutoScalingInstances"
                Resource: "*"
        - PolicyName: "CloudFormationDescribeStackResourcesPolicy"
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: "Allow"
                Action:
                  - "cloudformation:DescribeStackResources"
                Resource: "*"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - ec2.amazonaws.com  # Allow EC2 instances to assume the role
            Action:
              - "sts:AssumeRole"
          - Effect: Allow
            Principal:
              AWS: arn:aws:iam::AWS_ACCOUNT_ID:user/*  # Allow all users in the AWS account
            Action:
              - "sts:AssumeRole"
            Condition:
              StringLike:
                "aws:PrincipalTag/Email": "*@miles.systems"  # Restrict to users with an email ending in @miles.systems
      Path: "/"
      ManagedPolicyArns:
        - "arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore"

  EC2InstanceProfileForSSM:
    Type: AWS::IAM::InstanceProfile
    Properties:
      InstanceProfileName: "EC2RoleForSSM"
      Path: "/"
      Roles:
        - Ref: "EC2RoleForSSM"

  EC2RoleForImageBuilder:
    Type: "AWS::IAM::Role"
    Properties:
      RoleName: "EC2RoleForImageBuilder"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - ec2.amazonaws.com
            Action:
              - "sts:AssumeRole"
      Path: "/"
      ManagedPolicyArns:
        - "arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore"
        - "arn:aws:iam::aws:policy/EC2InstanceProfileForImageBuilderECRContainerBuilds"
        - "arn:aws:iam::aws:policy/EC2InstanceProfileForImageBuilder"
        - "arn:aws:iam::aws:policy/AmazonRDSFullAccess"

  EC2InstanceProfileForImageBuilder:
    Type: "AWS::IAM::InstanceProfile"
    Properties:
      InstanceProfileName: "EC2RoleForImageBuilder"
      Path: "/"
      Roles:
        - Ref: "EC2RoleForImageBuilder"
